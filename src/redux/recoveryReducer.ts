// Reducer
export const recoveryReducer = (state = initialState, action: any) => {
    switch (action.type) {
        default:
            return state
    }
}

// AC

// TC
// State && Types
const initialState = {}